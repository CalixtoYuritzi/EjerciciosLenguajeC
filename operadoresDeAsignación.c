#include <stdio.h>

int main(){
	
	int a; //b, c;
	
	//a=b=c= 10;
	
	a= 10;
	// Los Operadores de Asignación
	a += 10; //a= a + 10; 20
	a -= 5; // a = a - 5; 15
	a *= 2; // a = a * 2; 30
	a /= 2; // a = a / 2; 15
	
	printf("El valor de a es: %i", a);
	
	//printf("El valor de a, b, c es: %i %i %i", a, b, c);
	
	
	return 0;
}
